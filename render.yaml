services:
  - type: web
    name: restaurant-management-system-server
    runtime: node
    plan: free
    buildCommand: npm install
    preDeployCommand: npm run db:deploy
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
